CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
CONFIG_ZMK_WPM=y

# nice_view (core) provides the ls0xx display node/chosen zephyr,display,
# but its own built-in status widget must stay off so it doesn't collide
# with this shield's own zmk_display_status_screen_init()
CONFIG_NICE_VIEW_WIDGET_STATUS=n